# Быстрый старт

## Запуск приложения

1. **Запустите бэкенд сервис:**
```bash
cd /Users/vovkes/CoreML
source venv/bin/activate
uvicorn main:app --reload
```

2. **В другом терминале запустите Flutter приложение:**
```bash
cd /Users/vovkes/CoreML/flutter_app
flutter run -d macos
```

## Использование

1. **Отправка сообщения:**
   - Введите вопрос в поле ввода
   - Нажмите Enter или кнопку отправки

2. **Загрузка документа:**
   - Нажмите кнопку 📎 (прикрепить файл)
   - Выберите документ (PDF, DOCX, TXT и т.д.)
   - Документ автоматически обработается и добавится в базу знаний

3. **Настройки:**
   - 📚 RAG - включить/выключить использование загруженных документов
   - ⚖️ ZakonOnline - включить/выключить доступ к судебной практике
   - 🗑️ Очистить чат - удалить все сообщения

## Требования

- Бэкенд должен быть доступен на `http://localhost:8000`
- Для обработки документов должен быть запущен Celery worker:
```bash
celery -A core.celery_app worker --loglevel=info
```

## Troubleshooting

Если приложение не подключается к бэкенду:
- Проверьте, что бэкенд запущен
- Проверьте URL в `lib/main.dart` (по умолчанию `http://localhost:8000`)

