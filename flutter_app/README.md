# CoreML RAG MCP Chat - Flutter приложение для macOS

Flutter приложение для работы с CoreML RAG MCP сервисом. Позволяет:
- Общаться с AI через чат
- Загружать документы в любом формате
- Использовать данные из ZakonOnline через MCP
- Использовать загруженные документы в RAG системе

## Возможности

- 💬 **Чат интерфейс** - общение с AI ассистентом
- 📄 **Загрузка документов** - поддержка любых форматов (PDF, DOCX, TXT и т.д.)
- 🔍 **RAG поиск** - использование загруженных документов для ответов
- ⚖️ **ZakonOnline интеграция** - доступ к судебной практике через MCP
- 📊 **Отслеживание статуса** - мониторинг обработки документов

## Требования

- Flutter SDK 3.10.4 или выше
- macOS 10.14 или выше
- Запущенный бэкенд сервис на `http://localhost:8000`

## Установка

1. Установите зависимости:
```bash
flutter pub get
```

2. Убедитесь, что бэкенд сервис запущен:
```bash
cd ..
source venv/bin/activate
uvicorn main:app --reload
```

3. Запустите приложение:
```bash
flutter run -d macos
```

## Использование

### Чат

1. Введите вопрос в поле ввода
2. Нажмите кнопку отправки или Enter
3. Ответ появится в чате с указанием источников (RAG, MCP_Law)

### Загрузка документов

1. Нажмите кнопку прикрепления файла (📎)
2. Выберите документ любого формата
3. Документ будет автоматически обработан и добавлен в базу знаний
4. Статус обработки отображается в списке документов

### Настройки

- **RAG** (кнопка с книгой) - включить/выключить использование загруженных документов
- **ZakonOnline** (кнопка с молотком) - включить/выключить доступ к судебной практике
- **Очистить чат** (кнопка с корзиной) - удалить все сообщения

## Структура проекта

```
lib/
├── main.dart                 # Точка входа приложения
├── models/                  # Модели данных
│   ├── message.dart         # Модель сообщения
│   └── document.dart        # Модель документа
├── services/                # Сервисы
│   ├── api_client.dart      # HTTP клиент для API
│   └── chat_service.dart    # Сервис управления чатом
├── screens/                 # Экраны
│   └── chat_screen.dart     # Главный экран чата
└── widgets/                 # Виджеты
    ├── chat_message.dart    # Виджет сообщения
    └── document_list.dart   # Виджет списка документов
```

## API Endpoints

Приложение использует следующие endpoints бэкенда:

- `POST /query` - отправка запроса
- `POST /query/stream` - потоковая отправка запроса
- `POST /rag/add-document` - загрузка документа
- `GET /rag/task/{task_id}` - проверка статуса задачи
- `GET /rag/search` - поиск в RAG
- `GET /health` - проверка здоровья сервиса

## Форматы документов

Поддерживаются все форматы, которые обрабатывает бэкенд:
- PDF
- DOCX, DOC
- TXT
- MD
- И другие текстовые форматы

Документы автоматически распознаются и извлекается текст на бэкенде.

## Разработка

### Запуск в режиме разработки

```bash
flutter run -d macos --debug
```

### Сборка для релиза

```bash
flutter build macos --release
```

## Troubleshooting

### Приложение не подключается к бэкенду

1. Убедитесь, что бэкенд запущен на `http://localhost:8000`
2. Проверьте настройки файрвола
3. Проверьте логи бэкенда

### Документы не загружаются

1. Проверьте, что Celery worker запущен
2. Проверьте логи бэкенда на наличие ошибок
3. Убедитесь, что формат файла поддерживается

### Ошибки при установке зависимостей

```bash
flutter clean
flutter pub get
```

## Лицензия

См. основной README проекта.
