# Сохранение API ключей при развертывании

## Обзор

При повторном развертывании CoreML автоматически сохраняются все существующие API ключи и важные настройки из `.env` файла.

## Сохраняемые ключи

Следующие ключи **НЕ перезаписываются** при генерации паролей:

- ✅ `OPENAI_API_KEY` - ключ OpenAI API
- ✅ `MCP_LAW_API_KEY` - ключ MCP Law сервера
- ✅ `LMSTUDIO_BASE_URL` - URL для LM Studio
- ✅ `CUSTOM_LLM_BASE_URL` - URL для кастомного LLM

## Генерируемые ключи

Следующие ключи **генерируются автоматически** (только если отсутствуют или пустые):

- 🔐 `REDIS_PASSWORD` - пароль для Redis
- 🔐 `QDRANT_API_KEY` - API ключ для Qdrant

## Как это работает

### При первом развертывании:

1. Создается `.env` из `.env.example`
2. Генерируются пароли для Redis и Qdrant
3. Пользователь должен добавить `OPENAI_API_KEY` вручную

### При повторном развертывании:

1. Скрипт сохраняет существующие API ключи
2. Генерируются только отсутствующие пароли
3. Сохраненные ключи восстанавливаются после генерации

## Проверка ключей

Перед развертыванием можно проверить наличие всех ключей:

```bash
./scripts/verify_env.sh
```

Вывод:
```
🔍 Проверка важных ключей в .env...

✅ OPENAI_API_KEY: установлен (sk-p...AdsJ)
✅ REDIS_PASSWORD: установлен (KiHM...2hI9)
✅ QDRANT_API_KEY: установлен (72f9...b155)
✅ MCP_LAW_API_KEY: установлен (your...here)

✅ Все важные ключи установлены!
```

## Пример использования

### Первое развертывание:

```bash
# 1. Создание .env
cp .env.example .env

# 2. Добавление OPENAI_API_KEY
nano .env
# Добавьте: OPENAI_API_KEY=sk-your-actual-key-here

# 3. Развертывание (пароли сгенерируются автоматически)
./scripts/deploy.sh prod
```

### Повторное развертывание:

```bash
# OPENAI_API_KEY автоматически сохранится
./scripts/deploy.sh prod
```

## Ручное сохранение ключей

Если нужно сохранить ключи вручную перед обновлением:

```bash
# Сохранить текущие ключи
grep "^OPENAI_API_KEY=" .env > saved_keys.txt
grep "^MCP_LAW_API_KEY=" .env >> saved_keys.txt

# После обновления восстановить
cat saved_keys.txt >> .env
```

## Устранение неполадок

### Проблема: OPENAI_API_KEY потерян после развертывания

**Решение:**
1. Проверьте backup файлы: `ls -la .env.bak*`
2. Восстановите из backup: `grep OPENAI_API_KEY .env.bak`
3. Или добавьте заново в `.env`

### Проблема: Ключи не сохраняются

**Решение:**
1. Убедитесь, что ключи установлены ДО запуска `deploy.sh`
2. Проверьте формат: `OPENAI_API_KEY=your_key_here` (без пробелов вокруг `=`)
3. Запустите проверку: `./scripts/verify_env.sh`

## Безопасность

⚠️ **ВАЖНО:**
- Файл `.env` содержит секретные ключи
- Не коммитьте `.env` в git
- Используйте `.env.example` как шаблон
- Храните backup `.env` в безопасном месте

## Дополнительная информация

- Генерация секретов: `SECRETS_GENERATION.md`
- Безопасность: `SECURITY.md`
- Развертывание: `DEPLOYMENT_GUIDE.md`

