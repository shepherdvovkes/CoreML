# Исправление обработки документов без текста при множественной загрузке

## 🐛 Проблема

При загрузке нескольких документов, которые не содержат текста:
- Документы загружались, но статус не обновлялся правильно
- Документы оставались в статусе "processing" или показывались как успешно обработанные
- Не было понятного сообщения об ошибке для пользователя

## 🔍 Причина

**Проблема 1:** Когда документ не содержит текста, сервер возвращает:
```python
{
    "status": "error",
    "message": "Could not extract text from document",
    "chunks_count": 0
}
```

Но задача Celery завершается успешно (`SUCCESS`), потому что она выполнилась без исключений. Клиент проверял только статус задачи Celery (`success`), но не проверял статус внутри результата (`status: "error"`).

**Проблема 2:** Недостаточное логирование при множественной загрузке затрудняло отладку.

## ✅ Исправления

### 1. Обработка статуса "error" внутри результата задачи

**Было:**
```dart
} else if (taskStatus == 'success' || taskStatus == 'SUCCESS') {
  final result = status['result'] as Map<String, dynamic>?;
  // Сразу помечаем как ready, не проверяя статус внутри result
  _documents[docIndex] = _documents[docIndex].copyWith(
    status: DocumentStatus.ready,
    ...
  );
}
```

**Стало:**
```dart
} else if (taskStatus == 'success' || taskStatus == 'SUCCESS') {
  final result = status['result'] as Map<String, dynamic>?;
  if (result != null) {
    // Проверяем статус внутри результата задачи
    final resultStatus = result['status'] as String?;
    
    if (resultStatus == 'error') {
      // Задача выполнилась, но обработка документа завершилась с ошибкой
      final errorMessage = result['message']?.toString() ?? 'Ошибка обработки документа';
      _documents[docIndex] = _documents[docIndex].copyWith(
        status: DocumentStatus.error,
        error: errorMessage,
        statusMessage: errorMessage,
      );
      _messages.add(Message(
        content: 'Документ "${_documents[docIndex].name}": $errorMessage',
        type: MessageType.system,
        ...
      ));
      // Останавливаем проверку статуса
      timer.cancel();
      _documentStatusTimers.remove(documentId);
      return;
    }
    
    // Успешная обработка
    ...
  }
}
```

### 2. Улучшенное логирование

Добавлено детальное логирование для отладки множественной загрузки:
- Количество полученных результатов vs количество документов
- Список всех документов в Map
- Детали каждого результата (filename, task_id)
- Доступные имена файлов при ошибке сопоставления

```dart
AppLogger.i('Received ${results.length} task results for ${documents.length} documents');
AppLogger.d('Document in map: ${doc.name} -> ${doc.id}');
AppLogger.d('Processing result $i: filename=$filename, task_id=$individualTaskId');
AppLogger.w('Document not found by filename: $filename. Available: ${documentsByFilename.keys.join(", ")}');
```

## 📊 Результат

Теперь при загрузке документов без текста:
- ✅ Документ правильно помечается как ошибка
- ✅ Пользователь видит понятное сообщение: "Could not extract text from document"
- ✅ Проверка статуса останавливается для этого документа
- ✅ Другие документы продолжают обрабатываться независимо
- ✅ Улучшенное логирование помогает отладить проблемы

## 🧪 Тестирование

Для проверки исправления:

1. Загрузите несколько документов, включая документы без текста (пустые PDF, изображения без текста и т.д.)
2. Проверьте, что документы без текста помечаются как ошибка
3. Проверьте, что сообщение об ошибке отображается в чате
4. Проверьте логи - должны быть сообщения о статусе каждого документа

## 📝 Измененные файлы

- `flutter_app/lib/services/chat_service.dart`
  - Обновлен метод `_checkDocumentStatus()` для проверки статуса внутри результата
  - Добавлено логирование в метод `uploadDocuments()`

