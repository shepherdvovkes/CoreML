# Инициализация баз данных при развертывании

## Обзор

При развертывании CoreML автоматически создаются все необходимые коллекции/таблицы в базах данных и проверяется доступ пользователя из конфигурации.

## Автоматическая инициализация

### При развертывании

Скрипт `deploy.sh` автоматически:
1. Запускает контейнеры с БД (Redis, Qdrant)
2. Ожидает их готовности
3. Запускает скрипт инициализации `init_db.py`
4. Создает все необходимые коллекции
5. Проверяет доступ и права пользователя

```bash
./scripts/deploy.sh prod
```

### Ручная инициализация

Если нужно инициализировать БД вручную:

```bash
# Локально (вне контейнера)
python3 scripts/init_db.py

# Или в контейнере API
docker exec coreml_api python scripts/init_db.py
```

## Что инициализируется

### 1. Qdrant (Векторная БД)

**Создается:**
- Коллекция `legal_documents` (название из `QDRANT_COLLECTION_NAME`)
- Настройки векторов: размерность из embedding модели, расстояние COSINE

**Проверяется:**
- Доступ к Qdrant API
- Наличие API ключа (если настроен)
- Существование коллекции

**Пример:**
```python
# Автоматически создается при первом обращении
vector_store = create_vector_store()
# Коллекция создана!
```

### 2. Redis (Кэш и Celery)

**Проверяется:**
- Доступ к Redis
- Работа записи/чтения
- Наличие пароля (если настроен)

**Тест:**
- Запись тестового ключа
- Чтение тестового ключа
- Удаление тестового ключа

### 3. ChromaDB (Fallback)

**Создается:**
- Коллекция `legal_documents` (если используется ChromaDB вместо Qdrant)

**Проверяется:**
- Права на запись в директорию данных
- Существование коллекции

## Проверка доступа пользователя

### Qdrant

**API ключ:**
- Читается из `QDRANT_API_KEY` в `.env`
- Используется при подключении к Qdrant
- Если не установлен, работает без аутентификации

**Проверка:**
```python
client = QdrantClient(
    url=settings.qdrant_url,
    api_key=settings.qdrant_api_key if settings.qdrant_api_key else None
)
```

### Redis

**Пароль:**
- Читается из `REDIS_PASSWORD` в `.env`
- Включается в URL: `redis://:PASSWORD@host:port/db`
- Используется во всех подключениях (кэш, Celery)

**Проверка:**
```python
# URL автоматически содержит пароль
cache_service = CacheService(redis_url=settings.redis_url)
# settings.redis_url = "redis://:password@redis:6379/0"
```

## Структура коллекций

### Qdrant Collection: `legal_documents`

```python
{
    "name": "legal_documents",
    "vectors": {
        "size": 384,  # Размерность embedding модели
        "distance": "Cosine"
    }
}
```

**Payload структура:**
```python
{
    "text": "Текст документа",
    "embedding_model": "sentence-transformers/...",
    "embedding_model_version": "1.0.0",
    "indexed_at": "2024-01-01T00:00:00",
    "source": "/path/to/file.pdf",
    "migration_history": []
}
```

### ChromaDB Collection: `legal_documents`

Аналогичная структура, но хранится локально в `./data/vector_db`

## Логи инициализации

Скрипт `init_db.py` выводит подробные логи:

```
🚀 Начало инициализации баз данных...

🔧 Инициализация Qdrant...
   URL: http://qdrant:6333
   Collection: legal_documents
✅ Коллекция 'legal_documents' существует
✅ Qdrant инициализирован успешно

🔧 Инициализация Redis...
   URL: redis://:****@redis:6379/0
✅ Redis доступен и работает
✅ Redis: запись и чтение работают корректно

🔍 Проверка прав доступа...
✅ Qdrant API ключ установлен
✅ Redis пароль установлен в URL
✅ Права на запись в директорию data

📊 Результаты инициализации:
   ✅ QDRANT: OK
   ✅ REDIS: OK
   ✅ PERMISSIONS: OK

✅ Все базы данных инициализированы успешно!
```

## Устранение неполадок

### Проблема: Коллекция не создается

**Решение:**
```bash
# Проверьте доступ к Qdrant
curl http://localhost:6333/health

# Проверьте API ключ
grep QDRANT_API_KEY .env

# Запустите инициализацию вручную
python3 scripts/init_db.py
```

### Проблема: Redis недоступен

**Решение:**
```bash
# Проверьте Redis
docker exec coreml_redis redis-cli ping

# Проверьте пароль в URL
grep REDIS_URL .env

# Проверьте логи
docker-compose -f docker-compose.prod.yml logs redis
```

### Проблема: Нет прав на запись

**Решение:**
```bash
# Проверьте права на директорию
ls -la data/

# Установите права
chmod 755 data/
chown -R $(whoami) data/
```

## Интеграция в CI/CD

Для автоматического развертывания добавьте в pipeline:

```yaml
# Пример для GitHub Actions
- name: Initialize databases
  run: |
    docker-compose -f docker-compose.prod.yml up -d redis qdrant
    sleep 10
    python3 scripts/init_db.py
```

## Безопасность

### Рекомендации:

1. **API ключи и пароли:**
   - Храните в `.env` (не в git)
   - Используйте secrets management для production

2. **Права доступа:**
   - Минимальные права для пользователя приложения
   - Отдельный пользователь для БД (если возможно)

3. **Сетевая изоляция:**
   - БД доступны только внутри Docker network
   - Не открывайте порты БД публично

## Дополнительная информация

- Руководство по развертыванию: `DEPLOYMENT_GUIDE.md`
- Безопасность: `SECURITY.md`
- Генерация секретов: `SECRETS_GENERATION.md`

