# Проверка Flutter клиента

## ✅ Что работает хорошо

### 1. Поддержка HTML файлов
- ✅ HTML файлы включены в `supportedFileTypes` (строки 15-16 в `config.dart`)
- ✅ Поддерживаются форматы: `html`, `htm`
- ✅ Валидация формата файла работает корректно

### 2. Загрузка документов
- ✅ Метод `uploadDocument()` для одного файла
- ✅ Метод `uploadDocumentsBatch()` для пакетной загрузки
- ✅ Используются правильные endpoints: `/rag/add-document` и `/rag/add-documents-batch`
- ✅ Поддержка прогресса загрузки через `onProgress` callback

### 3. Валидация
- ✅ Проверка размера файла (максимум 100 MB)
- ✅ Проверка формата файла
- ✅ Проверка пути к файлу
- ✅ Валидация ответа сервера

### 4. Обработка ошибок
- ✅ Централизованная обработка через `_handleDioException()`
- ✅ Специальная обработка для разных HTTP статусов (401, 403, 404, 413, 429, 5xx)
- ✅ Retry логика для сетевых ошибок и временных ошибок сервера
- ✅ Правильная обработка ошибок загрузки файлов

### 5. Отслеживание статуса
- ✅ Отслеживание статуса загрузки документов
- ✅ Проверка статуса обработки через `/rag/task/{task_id}`
- ✅ Обновление UI при изменении статуса

## ⚠️ Потенциальные проблемы

### 1. Таймауты для загрузки файлов
**Текущая настройка:**
```dart
static const Duration connectionTimeout = Duration(seconds: 30);
static const Duration receiveTimeout = Duration(seconds: 30);
```

**Проблема:**
- Для больших HTML файлов (особенно с конвертацией через Convert API) 30 секунд может быть недостаточно
- Convert API имеет таймаут 300 секунд (5 минут) на бэкенде
- При конвертации HTML в текст может потребоваться больше времени

**Рекомендация:**
```dart
// В config.dart
static const Duration uploadTimeout = Duration(minutes: 5); // Для загрузки файлов
static const Duration connectionTimeout = Duration(seconds: 30); // Для обычных запросов
static const Duration receiveTimeout = Duration(seconds: 30); // Для обычных запросов
```

И использовать специальный таймаут при загрузке:
```dart
final response = await _dio.post(
  '/rag/add-document',
  data: formData,
  options: Options(
    sendTimeout: AppConfig.uploadTimeout,
    receiveTimeout: AppConfig.uploadTimeout,
  ),
  onSendProgress: onProgress,
);
```

### 2. Обработка кодировки HTML файлов
**Проблема:**
- HTML файлы могут быть в разных кодировках (UTF-8, Windows-1251, ISO-8859-1)
- Flutter клиент не указывает кодировку при загрузке
- Бэкенд должен автоматически определять кодировку, но это может не всегда работать

**Рекомендация:**
- Бэкенд уже использует Convert API с поддержкой кодировок
- Можно добавить параметр `encoding` в будущем, если понадобится

### 3. Retry для больших файлов
**Проблема:**
- Retry логика может повторять загрузку больших файлов
- Это может привести к излишней нагрузке на сеть

**Текущая реализация:**
- Retry работает только для ошибок подключения и временных ошибок сервера (5xx)
- Это правильное поведение, но можно улучшить:
  - Не делать retry для ошибок 413 (файл слишком большой)
  - Не делать retry для ошибок 400 (неверный формат)

### 4. Прогресс загрузки
**Текущая реализация:**
- Поддержка `onProgress` callback есть
- Но не используется в UI для отображения прогресса

**Рекомендация:**
- Добавить отображение прогресса загрузки в UI
- Показывать процент загруженных данных для больших файлов

## 📋 Проверка интеграции с Convert API

### На бэкенде:
- ✅ Convert API клиент настроен правильно
- ✅ Endpoint: `https://mail.s0me.uk/convert/v1/api/convert-to-text`
- ✅ API ключ настроен
- ✅ Поддержка кодировок (utf8, windows-1251, iso-8859-1)

### На Flutter клиенте:
- ✅ HTML файлы поддерживаются
- ✅ Загрузка через `/rag/add-document`
- ⚠️ Таймауты могут быть недостаточными для конвертации

## 🔧 Рекомендации по улучшению

### 1. Увеличить таймауты для загрузки
```dart
// В config.dart
static const Duration uploadConnectionTimeout = Duration(minutes: 1);
static const Duration uploadReceiveTimeout = Duration(minutes: 6); // Больше чем Convert API timeout
```

### 2. Добавить отображение прогресса
```dart
// В chat_service.dart при загрузке
await _apiClient.uploadDocument(
  file,
  onProgress: (sent, total) {
    final progress = (sent / total * 100).toInt();
    // Обновить UI с прогрессом
  },
);
```

### 3. Улучшить обработку ошибок для больших файлов
```dart
// В api_client.dart
if (statusCode == 413) {
  return FileError('Файл слишком большой. Максимальный размер: ${AppConfig.maxFileSize ~/ (1024 * 1024)} MB');
}
```

### 4. Добавить логирование для отладки
```dart
AppLogger.d('Uploading file: ${file.path}, size: ${fileSize} bytes, type: $extension');
```

## ✅ Итоговая оценка

**Общая оценка: 8/10**

**Сильные стороны:**
- ✅ Хорошая структура кода
- ✅ Правильная обработка ошибок
- ✅ Поддержка HTML файлов
- ✅ Retry логика
- ✅ Валидация данных

**Что нужно улучшить:**
- ⚠️ Таймауты для загрузки больших файлов
- ⚠️ Отображение прогресса загрузки в UI
- ⚠️ Логирование для отладки

## 🧪 Тестирование

Для проверки работы с HTML файлами:

1. **Загрузить HTML файл через Flutter приложение**
   - Выбрать HTML файл
   - Проверить, что он загружается без ошибок
   - Проверить, что статус меняется на "processing" → "success"

2. **Проверить обработку на бэкенде**
   - HTML должен быть обработан через Convert API
   - Текст должен быть извлечен корректно
   - Документ должен быть добавлен в RAG систему

3. **Проверить с большими HTML файлами**
   - Загрузить HTML файл > 1 MB
   - Проверить, что таймауты достаточны
   - Проверить, что прогресс отображается (если реализовано)

