# Быстрый старт - Кнопки приложений

## ✅ Что было сделано

1. ✅ Создана директория `/static/badges/` для хранения кнопок
2. ✅ Скачаны официальные кнопки для всех платформ:
   - Google Play (Android) - 3 языка (en, ru, uk)
   - Apple App Store (iOS/macOS) - 3 языка (en, ru, uk)
   - Microsoft Store (Windows) - 3 языка (en, ru, uk)
   - Flathub (Linux) - 3 языка (en, ru, uk)
3. ✅ Создана HTML страница `index.html` с красивым дизайном
4. ✅ Создан скрипт развертывания `deploy.sh`

## 🚀 Размещение на lexapp.co.ua

### Вариант 1: Автоматическое развертывание (рекомендуется)

```bash
cd static/badges
./deploy.sh user@lexapp.co.ua
```

Скрипт автоматически:
- Создаст директорию на сервере
- Скопирует все файлы
- Установит правильные права доступа

### Вариант 2: Ручное развертывание

```bash
# 1. Скопируйте директорию на сервер
scp -r static/badges/ user@lexapp.co.ua:/var/www/lexapp.co.ua/badges/

# 2. На сервере установите права
ssh user@lexapp.co.ua
sudo chmod -R 755 /var/www/lexapp.co.ua/badges
sudo chown -R www-data:www-data /var/www/lexapp.co.ua/badges
```

### Настройка nginx

Добавьте в конфигурацию nginx для lexapp.co.ua:

```nginx
server {
    listen 80;
    listen [::]:80;
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    
    server_name lexapp.co.ua www.lexapp.co.ua;
    
    root /var/www/lexapp.co.ua;
    index index.html;
    
    # Статические файлы для кнопок
    location /badges {
        alias /var/www/lexapp.co.ua/badges;
        try_files $uri $uri/ =404;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
    
    # SSL конфигурация (если используется)
    # ssl_certificate /path/to/cert.pem;
    # ssl_certificate_key /path/to/key.pem;
}
```

После настройки перезапустите nginx:
```bash
sudo nginx -t  # Проверка конфигурации
sudo systemctl reload nginx
```

## 📝 Важно: Обновите ссылки!

В файле `index.html` все ссылки ведут на `#`. Замените их на реальные ссылки:

1. Откройте `index.html`
2. Найдите все `<a href="#">` 
3. Замените на реальные ссылки:
   - Google Play: `https://play.google.com/store/apps/details?id=YOUR_APP_ID`
   - App Store: `https://apps.apple.com/app/idYOUR_APP_ID`
   - Microsoft Store: `https://www.microsoft.com/store/productId/YOUR_PRODUCT_ID`
   - Flathub: `https://flathub.org/apps/YOUR_APP_ID`

## 🌐 Доступ к странице

После развертывания страница будет доступна по адресу:
- `https://lexapp.co.ua/badges/index.html`
- Или просто `https://lexapp.co.ua/badges/` (если настроен index)

## 📱 Использование кнопок на других страницах

Вы можете использовать отдельные кнопки на любых страницах сайта:

```html
<!-- Пример для украинской версии -->
<a href="YOUR_GOOGLE_PLAY_LINK" target="_blank">
    <img src="/badges/google-play-badge-uk.png" alt="Доступно в Google Play" style="height: 60px;">
</a>
```

## 🔄 Обновление кнопок

Если нужно обновить кнопки до последних версий, выполните:

```bash
cd static/badges
# См. команды в README.md
```

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте права доступа к файлам на сервере
2. Убедитесь, что nginx правильно настроен
3. Проверьте логи nginx: `sudo tail -f /var/log/nginx/error.log`
