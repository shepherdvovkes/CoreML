# Готовность к загрузке документов

## ✅ Сервер запущен

- **URL**: http://127.0.0.1:8000
- **PID**: $(ps aux | grep -E "uvicorn.*main:app" | grep -v grep | awk '{print $2}' | head -1)
- **Статус**: Работает

## 📤 Endpoints для загрузки документов

### 1. Загрузка одного документа
**POST** `/rag/add-document`

```bash
curl -X POST "http://127.0.0.1:8000/rag/add-document" \
  -F "file=@/path/to/document.pdf"
```

**Ответ:**
```json
{
  "status": "queued",
  "task_id": "abc123-def456-...",
  "message": "Document queued for processing",
  "check_status_url": "/rag/task/abc123-def456-..."
}
```

### 2. Пакетная загрузка
**POST** `/rag/add-documents-batch`

```bash
curl -X POST "http://127.0.0.1:8000/rag/add-documents-batch" \
  -F "files=@doc1.pdf" \
  -F "files=@doc2.pdf" \
  -F "files=@doc3.pdf"
```

### 3. Проверка статуса обработки
**GET** `/rag/task/{task_id}`

```bash
curl "http://127.0.0.1:8000/rag/task/abc123-def456-..."
```

**Статусы:**
- `pending` - в очереди
- `processing` - обрабатывается
- `success` - успешно обработан
- `failure` - ошибка

## 📋 Поддерживаемые форматы

Система поддерживает:
- PDF файлы
- Текстовые файлы (.txt, .md)
- Документы Word (.docx)
- HTML документы (.html, .htm)
- Excel файлы (.xlsx, .xls)
- Другие форматы через DocumentProcessor

## ⚠️ Текущий статус системы

- **Векторное хранилище**: DummyVectorStore (Qdrant недоступен)
- **Кэш**: Отключен (Redis недоступен)
- **Обработка документов**: Работает через Celery

**Примечание**: Документы будут обработаны и добавлены в очередь, но сохранение в векторное хранилище будет работать только после восстановления Qdrant или ChromaDB.

## 🔍 API Документация

Полная документация доступна по адресу:
http://127.0.0.1:8000/docs

## 📝 Пример использования (Python)

```python
import httpx

async with httpx.AsyncClient() as client:
    with open("document.pdf", "rb") as f:
        files = {"file": ("document.pdf", f, "application/pdf")}
        response = await client.post(
            "http://127.0.0.1:8000/rag/add-document",
            files=files
        )
        result = response.json()
        print(f"Task ID: {result['task_id']}")
```

## 🎯 Готовность

✅ Сервер запущен
✅ Endpoints доступны
✅ Обработка документов настроена
⚠️ Векторное хранилище в режиме fallback (DummyVectorStore)
