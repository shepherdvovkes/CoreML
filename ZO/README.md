# 📚 Документація по скриптам для роботи з API ZakonOnline

Ця папка містить набір скриптів для роботи з API судових рішень України через сервіс ZakonOnline.

## 🔧 Налаштування

### Змінні оточення

Створіть файл `.env` в корені проекту з наступним вмістом:

```env
ZAKON_TOKEN=your_zakon_online_api_token_here
```

### Встановлення залежностей

```bash
npm install axios dotenv html-to-text
```

---

## 📋 Огляд скриптів

| Скрипт | Призначення | Вхідні дані | Вихідні дані |
|--------|-------------|-------------|--------------|
| `query_Zakon_Search_ByQueries.js` | Пошук рішень за ключовими словами | Масив запитів | JSON з результатами пошуку |
| `fetchFullText_ByDocId.js` | Завантаження повного тексту по ID | ID документа | JSON + TXT файли |
| `fetchFullText_ByCaseNumber.js` | Завантаження всіх документів справи | Номер справи | JSON + TXT для кожного документа |
| `query_Zakon_getAllResolutions_ByCaseNumber.js` | Отримання резолюцій по справі | Номер справи | JSON з резолютивними частинами |
| `query_Zakon_WithResolution_fromFile.js` | Збагачення існуючих даних резолюціями | Файл result.json | JSON з доданими резолюціями |
| `query_Zakon_WithResolution_single.js` | Отримання резолюції одного документа | ID документа | JSON з резолюцією |
| `query_Zakon_MurderCases_Statistics.js` | Статистика уголовних справ з убивствами за роки | Роки (2022-2024) | JSON зі статистикою по роках |

---

## 📖 Детальний опис скриптів

### 1️⃣ `query_Zakon_Search_ByQueries.js`

**Призначення:** Семантичний пошук судових рішень за ключовими словами через власний API-сервіс.

**Що робить:**
- Виконує пошук за заданими запитами
- Автоматично обходить всі сторінки результатів
- Збирає всі знайдені документи в один файл
- Виводить інформацію про кожен документ у консоль

**Конфігурація:**

```javascript
const BASE_URL = "https://legal-gpt-service.onrender.com/get-legal-decisions";

const payloadTemplate = {
  query: [
    "про визнання особи такою, що втратила право користування житловим приміщенням",
    "приватизації",
    "зареєструвати місце проживання",
  ],
  paramsConfig: {
    mode: "sph04",
    target: "text",
    results: "standart",
    namespace: "sudreyestr",
    page: "1",
    limit: "10",
    instance: "3",      // Інстанція (1-перша, 2-апеляційна, 3-касаційна)
    judgement: "3",     // Тип рішення (3-рішення, 4-постанова, 5-ухвала)
  },
};
```

**Вихідні файли:**
- `search_results/result.json` - всі знайдені рішення

**Запуск:**

```bash
node scripts/query_Zakon_Search_ByQueries.js
```

**Приклад виводу:**

```
📤 Запит сторінки 1 з параметрами:
📦 Отримано 10 рішень на сторінці 1
  🔹 [1] Київський районний суд — Рішення про визнання особи...
  🔹 [2] Львівський апеляційний суд — Постанова про...
...
✅ Загальна кількість рішень: 47
💾 Усі результати збережено у: /path/to/search_results/result.json
```

---

### 2️⃣ `fetchFullText_ByDocId.js`

**Призначення:** Завантаження повного тексту конкретного судового рішення по його ID.

**Що робить:**
- Завантажує повний документ з API ZakonOnline
- Конвертує HTML в читабельний текст
- Зберігає результат у двох форматах (JSON та TXT)

**Конфігурація:**

```javascript
const OUTPUT_DIR = path.resolve(__dirname, "..", "resolutions_by_case", "320-55287-25");
const DOC_ID = 131831617; // ID документа для завантаження
const FILE_NAME_BASE = "320-55287-25"; // Базова назва файлів
```

**API Endpoint:**
```
GET https://court.searcher.api.zakononline.com.ua/v1/document/by/number/{DOC_ID}
```

**Вихідні файли:**
- `full_text_{FILE_NAME_BASE}.json` - повні дані документа
- `full_text_{FILE_NAME_BASE}.txt` - текстова версія (назва + резолюція + текст)

**Запуск:**

```bash
node scripts/fetchFullText_ByDocId.js
```

**Структура TXT файлу:**

```
📄 Ухвала суду від 17.11.2025 по справі № 320/55287/25

🧾 Про відкриття спрощеного провадження

📚 Повний текст:
[текст документа без HTML тегів]
```

---

### 3️⃣ `fetchFullText_ByCaseNumber.js`

**Призначення:** Завантаження повних текстів ВСІХ документів по номеру справи.

**Що робить:**
- Шукає всі документи по номеру справи
- Завантажує повний текст кожного документа
- Зберігає кожен документ окремо з інформативною назвою файлу
- Виводить детальну інформацію про кожен документ

**Конфігурація:**

```javascript
const causeNumber = "320/55287/25"; // Номер справи
const outputDir = path.resolve(
  __dirname,
  "..",
  "resolutions_by_case/320-55287-25"
);
```

**Формат імені файлів:**
```
{дата}_{тип_рішення}_{doc_id}.json
{дата}_{тип_рішення}_{doc_id}.txt

Приклад: 2025-11-17_5_131831617.json
```

**Типи рішень (judgment_code):**
- `3` - Рішення
- `4` - Постанова
- `5` - Ухвала
- `11` - Вирок

**Вихідні файли:**
- Для кожного документа: `{date}_{type}_{id}.json` та `{date}_{type}_{id}.txt`

**Запуск:**

```bash
node scripts/fetchFullText_ByCaseNumber.js
```

**Приклад виводу:**

```
🔍 Шукаю всі документи по справі 320/55287/25...

✅ Знайдено документів: 2

[1] Ухвала суду від 17.11.2025
    Дата: 2025-11-17T00:00:00.000Z
    Резолюція: Про відкриття спрощеного провадження
    Doc ID: 131831617
    Тип рішення: 5

[2] Рішення суду від 15.12.2025
    Дата: 2025-12-15T00:00:00.000Z
    Резолюція: Позов задоволено
    Doc ID: 132145678
    Тип рішення: 3

📥 [1/2] Завантажую документ 131831617...
   💾 JSON: 2025-11-17_5_131831617.json
   📝 TXT: 2025-11-17_5_131831617.txt

✅ Готово! Усі документи збережено
```

---

### 4️⃣ `query_Zakon_getAllResolutions_ByCaseNumber.js`

**Призначення:** Отримання **тільки резолютивних частин** всіх документів по справі.

**Що робить:**
- Шукає всі документи по номеру справи
- Для кожного документа отримує `expanded_resolution` (розширену резолюцію)
- Сортує документи за датою (нові спочатку)
- Зберігає компактний JSON з метаданими та резолюціями

**Конфігурація:**

```javascript
const causeNumbers = ["320/55287/25"]; // Можна вказати кілька справ
const outputDir = path.resolve(
  __dirname,
  "..",
  "resolutions_by_case/320-55287-25"
);
```

**API Endpoints:**
1. Пошук документів:
```
GET https://court.searcher.api.zakononline.com.ua/v1/search
    ?where[cause_num]=320/55287/25
    &mode=default
    &namespace=sudreyestr
    &limit=50
```

2. Отримання резолюції:
```
GET https://court.searcher.api.zakononline.com.ua/v1/document/expanded_resolution/{DOC_ID}
```

**Вихідні файли:**
- `{номер-справи}.json` - компактний файл з резолюціями

**Структура вихідного JSON:**

```json
[
  {
    "doc_id": 128163892,
    "title": "Ухвала суду від 17.11.2025...",
    "date": "2025-11-17T00:00:00.000Z",
    "url": "https://zakononline.ua/court-decisions/show/131831617",
    "expanded_resolution": "Відкрити провадження в адміністративній справі..."
  }
]
```

**Запуск:**

```bash
node scripts/query_Zakon_getAllResolutions_ByCaseNumber.js
```

**Приклад виводу:**

```
🔍 Обробка справи № 320/55287/25
📄 [1/1] "Ухвала суду від 17.11.2025..." (ID: 128163892)
✅ Збережено 1 резолюцій у файл: .../320-55287-25.json
```

**Коли використовувати:**
- Потрібна тільки резолютивна частина без повного тексту
- Швидкий аналіз результатів рішень
- Моніторинг статусу справи

---

### 5️⃣ `query_Zakon_WithResolution_fromFile.js`

**Призначення:** Збагачення існуючого файлу з результатами пошуку резолютивними частинами.

**Що робить:**
- Читає файл `search_results/result.json` з раніше знайденими документами
- Для кожного документа завантажує `expanded_resolution`
- Додає резолюції до існуючих даних
- Зберігає збагачений результат

**Конфігурація:**

```javascript
const INPUT_FILE = path.resolve(
  __dirname,
  "..",
  "search_results",
  "result.json"
);
const OUTPUT_FILE = path.resolve(
  __dirname,
  "..",
  "search_results",
  "result_with_resolution.json"
);
```

**Workflow:**

```
result.json                      result_with_resolution.json
    ↓                                       ↓
[{id, title, ...}]  →  API  →  [{id, title, ..., expanded_resolution}]
```

**Вихідні файли:**
- `search_results/result_with_resolution.json` - збагачений файл

**Запуск:**

```bash
# Спочатку запустіть пошук
node scripts/query_Zakon_Search_ByQueries.js

# Потім збагатіть резолюціями
node scripts/query_Zakon_WithResolution_fromFile.js
```

**Приклад виводу:**

```
🔎 [1/47] Получаем resolution для ID: 128163892
🔎 [2/47] Получаем resolution для ID: 127854321
...
✅ Готово! Файл сохранен: .../result_with_resolution.json
```

**Особливості:**
- Затримка 300ms між запитами (щоб не перевантажити API)
- Обробляє помилки для кожного документа окремо
- Не зупиняється при помилці одного документа

---

### 6️⃣ `query_Zakon_WithResolution_single.js`

**Призначення:** Швидке отримання резолютивної частини одного конкретного документа.

**Що робить:**
- Завантажує тільки резолюцію одного документа
- Виводить резолюцію в консоль
- Зберігає в окремий файл

**Конфігурація:**

```javascript
const docId = 113041637; // ID документа
const outputDir = path.resolve(__dirname, "..", "resolutions");
```

**API Endpoint:**
```
GET https://court.searcher.api.zakononline.com.ua/v1/document/expanded_resolution/{DOC_ID}
```

**Вихідні файли:**
- `resolutions/{doc_id}.json` - резолюція з метаданими

**Структура вихідного файлу:**

```json
{
  "doc_id": 113041637,
  "expanded_resolution": "Позов задоволено повністю...",
  "fetched_at": "2025-12-10T12:34:56.789Z"
}
```

**Запуск:**

```bash
node scripts/query_Zakon_WithResolution_single.js
```

**Приклад виводу:**

```
🔍 Получаем resolution для doc_id: 113041637
✅ Expanded resolution:

Позов задоволено повністю. Стягнути з відповідача на користь позивача...

💾 JSON сохранён в файл: .../resolutions/113041637.json
```

**Коли використовувати:**
- Швидка перевірка резолюції конкретного документа
- Тестування API
- Отримання резолюції для аналізу

---

### 7️⃣ `query_Zakon_MurderCases_Statistics.js`

**Призначення:** Статистичний аналіз кількості уголовних справ з убивствами (всі типи) за вказані роки.

**Що робить:**
- Шукає уголовні справи з убивствами за кожен рік окремо (2022, 2023, 2024)
- Використовує множину запитів для покриття всіх типів вбивств
- Видаляє дублікати справ
- Генерує статистику по роках
- Зберігає детальну інформацію про кожну справу

**Конфігурація:**

```javascript
const YEARS = [2022, 2023, 2024];

const MURDER_QUERIES = [
  "вбивство",
  "убивство",
  "вбивство з особливою жорстокістю",
  "вбивство за попередньою змовою",
  // ... інші типи
];
```

**Методи пошуку:**
1. **Семантичний пошук** (за замовчуванням) - через `legal-gpt-service.onrender.com`
2. **Прямий API** - через `court.searcher.api.zakononline.com.ua` (fallback)

**Вихідні файли:**
- `murder_cases_statistics/murders_2022.json` - справи за 2022 рік
- `murder_cases_statistics/murders_2023.json` - справи за 2023 рік
- `murder_cases_statistics/murders_2024.json` - справи за 2024 рік
- `murder_cases_statistics/summary.json` - загальна статистика

**Структура вихідного JSON:**

```json
{
  "years": {
    "2022": {
      "count": 150,
      "cases": [
        {
          "id": 123456,
          "title": "Вирок суду...",
          "date": "2022-03-15T00:00:00.000Z",
          "court": "Київський районний суд",
          "url": "https://zakononline.ua/..."
        }
      ]
    }
  },
  "total": 450,
  "method": "semantic"
}
```

**Запуск:**

```bash
node ZO/query_Zakon_MurderCases_Statistics.js
```

**Приклад виводу:**

```
🚀 Запуск статистики вбивств за 2022-2024 роки

🔍 Пошук вбивств за 2022 рік (семантичний)...
  🔎 [1/16] Запит: "вбивство"
  📄 Сторінка 1...
  ✅ Знайдено: 45 унікальних справ
  ...

📊 ПІДСУМОК СТАТИСТИКИ ВБИВСТВ
Кількість справ по роках:
  2022: 150 справ
  2023: 180 справ
  2024: 120 справ

📈 Всього унікальних справ: 450
```

**Особливості:**
- Автоматичне видалення дублікатів за ID документа
- Фільтрація за роком (adjudication_date)
- Фільтрація за типом рішення (judgement: "6" - Вирок)
- Фільтрація за інстанцією (instance: "1" - Перша інстанція)
- Автоматичне перемикання на прямий API при помилках семантичного пошуку
- Затримки між запитами для захисту API

**Коли використовувати:**
- Статистичний аналіз кримінальних справ
- Порівняння динаміки злочинів по роках
- Дослідження судової практики

---

## 🔍 Важлива інформація про ID документів

⚠️ **API ZakonOnline використовує ДВА різних типи ID:**

1. **`search_id`** (або просто `id`) - ID в індексі пошуку
2. **`doc_id`** - реальний ID документа в базі даних

**Приклад:**
```json
{
  "id": 128163892,        // search_id - використовується в пошуку
  "doc_id": 131831617,    // doc_id - використовується для завантаження
  "title": "Ухвала..."
}
```

**Правило:**
- Для пошуку та `expanded_resolution` → використовуйте `id` (search_id)
- Для завантаження повного тексту → використовуйте `doc_id`

---

## 📁 Структура вихідних даних

### Типова структура папок:

```
legal-gpt/
├── scripts/
│   ├── README.md                           # Ця документація
│   ├── query_Zakon_Search_ByQueries.js
│   ├── fetchFullText_ByDocId.js
│   └── ...
├── search_results/
│   ├── result.json                         # Результати пошуку
│   └── result_with_resolution.json         # З резолюціями
├── resolutions_by_case/
│   └── 320-55287-25/                       # Папка для кожної справи
│       ├── 320-55287-25.json               # Метадані + резолюції
│       ├── 2025-11-17_5_131831617.json     # Повні дані документа
│       ├── 2025-11-17_5_131831617.txt      # Текстова версія
│       └── РЕЗОЛЮТИВНА_ЧАСТИНА.txt         # Читабельна резолюція
└── resolutions/
    └── {doc_id}.json                       # Окремі резолюції
```

---

## 🚀 Типові сценарії використання

### Сценарій 1: Пошук рішень за темою

```bash
# 1. Налаштуйте запити в query_Zakon_Search_ByQueries.js
# 2. Запустіть пошук
node scripts/query_Zakon_Search_ByQueries.js

# 3. (Опціонально) Додайте резолюції
node scripts/query_Zakon_WithResolution_fromFile.js
```

### Сценарій 2: Завантаження всіх документів по справі

```bash
# 1. Встановіть номер справи в fetchFullText_ByCaseNumber.js
# 2. Запустіть скрипт
node scripts/fetchFullText_ByCaseNumber.js
```

### Сценарій 3: Отримання тільки резолюцій по справі

```bash
# 1. Встановіть номер справи в query_Zakon_getAllResolutions_ByCaseNumber.js
# 2. Запустіть скрипт
node scripts/query_Zakon_getAllResolutions_ByCaseNumber.js
```

### Сценарій 4: Швидка перевірка одного документа

```bash
# 1. Встановіть doc_id в query_Zakon_WithResolution_single.js
# 2. Запустіть скрипт
node scripts/query_Zakon_WithResolution_single.js
```

---

## ⚙️ Параметри API

### Типи рішень (judgment_code):

| Код | Тип рішення |
|-----|-------------|
| 3 | Рішення |
| 4 | Постанова |
| 5 | Ухвала |
| 6 | Вирок |
| 11 | Судовий наказ |

### Інстанції (instance):

| Код | Інстанція |
|-----|-----------|
| 1 | Перша інстанція |
| 2 | Апеляційна інстанція |
| 3 | Касаційна інстанція |

### Режими пошуку (mode):

- `default` - стандартний пошук
- `sph04` - семантичний пошук через Sphinx

### Типи результатів (results):

- `standart` - стандартні поля
- `extended` - розширена інформація

---

## 🛠️ Налагодження

### Перевірка токена:

```bash
echo $ZAKON_TOKEN  # Linux/Mac
echo %ZAKON_TOKEN% # Windows
```

### Тестовий запит:

```bash
curl -H "X-App-Token: YOUR_TOKEN" \
  "https://court.searcher.api.zakononline.com.ua/v1/search?where[cause_num]=320/55287/25&limit=1"
```

### Логування:

Всі скрипти виводять детальну інформацію в консоль:
- 🔍 - пошук
- 📥 - завантаження
- 💾 - збереження файлу
- ✅ - успішне виконання
- ❌ - помилка
- ⚠️ - попередження

---

## 📝 Примітки

1. **Обмеження API:** Рекомендується додавати затримки між запитами (300-500ms)
2. **Розмір відповіді:** Деякі документи можуть бути дуже великими (>1MB)
3. **Кодування:** Всі файли зберігаються в UTF-8
4. **HTML в резолюціях:** `expanded_resolution` містить HTML теги (`<br>`, тощо)

---

## 🔗 Корисні посилання

- [Документація API ZakonOnline](https://zakononline.com.ua/api)
- [Єдиний державний реєстр судових рішень](https://reyestr.court.gov.ua/)

---

## 📧 Підтримка

При виникненні проблем перевірте:
1. Наявність та валідність `ZAKON_TOKEN` в `.env`
2. Доступність API ZakonOnline
3. Правильність формату вхідних даних (номер справи, ID документа)

---

**Останнє оновлення:** 10 грудня 2025
**Версія:** 1.0

