# Исправление проблемы множественной загрузки файлов

## 🐛 Проблема

При множественной загрузке файлов:
- Загружался только последний файл
- Остальные файлы не обрабатывались
- Приложение не зависало, но ничего не происходило

## 🔍 Причина

**Основная проблема:** Использовался один общий таймер `_documentStatusTimer` для проверки статуса всех документов. При вызове `_checkDocumentStatus()` для нового документа, предыдущий таймер отменялся, и проверка статуса работала только для последнего документа.

**Дополнительные проблемы:**
1. Сопоставление документов с результатами происходило по индексу, что могло привести к неправильному сопоставлению
2. Не было проверки, все ли документы получили `task_id`
3. Таймеры не очищались при удалении документов

## ✅ Исправления

### 1. Отдельный таймер для каждого документа

**Было:**
```dart
Timer? _documentStatusTimer; // Один таймер для всех

void _checkDocumentStatus(String taskId, String documentId) {
  _documentStatusTimer?.cancel(); // Отменяет предыдущий таймер
  _documentStatusTimer = Timer.periodic(...);
}
```

**Стало:**
```dart
final Map<String, Timer> _documentStatusTimers = {}; // Отдельный таймер для каждого

void _checkDocumentStatus(String taskId, String documentId) {
  _documentStatusTimers[documentId]?.cancel(); // Отменяет только таймер этого документа
  final timer = Timer.periodic(...);
  _documentStatusTimers[documentId] = timer; // Сохраняем таймер
}
```

### 2. Улучшенное сопоставление документов с результатами

**Было:**
```dart
for (int i = 0; i < results.length && i < documents.length; i++) {
  final document = documents[i]; // По индексу
  // ...
}
```

**Стало:**
```dart
// Создаем Map для быстрого поиска по имени файла
final documentsByFilename = <String, Document>{};
for (final doc in documents) {
  documentsByFilename[doc.name] = doc;
}

// Сопоставляем по имени файла из ответа сервера
for (final resultData in results) {
  final filename = result?['filename'] as String?;
  final document = documentsByFilename[filename]; // По имени файла
  // ...
}
```

### 3. Проверка обработки всех документов

Добавлена проверка, что все документы получили `task_id`:

```dart
final processedDocuments = <String>{};
// ... обработка результатов ...

// Проверяем, все ли документы получили task_id
final unprocessedDocuments = documents.where(
  (d) => !processedDocuments.contains(d.id)
).toList();
if (unprocessedDocuments.isNotEmpty) {
  // Помечаем необработанные документы как ошибка
}
```

### 4. Очистка таймеров при удалении документов

```dart
void removeDocument(String documentId) {
  _documentStatusTimers[documentId]?.cancel();
  _documentStatusTimers.remove(documentId);
  // ...
}

Future<void> clearAllDocuments() async {
  for (final timer in _documentStatusTimers.values) {
    timer.cancel();
  }
  _documentStatusTimers.clear();
  // ...
}
```

### 5. Улучшенное логирование

Добавлено логирование для отладки:
- Когда начинается проверка статуса для документа
- Когда документ не найден
- Когда документ не получил `task_id`

## 📊 Результат

Теперь при множественной загрузке:
- ✅ Каждый файл получает свой `task_id`
- ✅ Каждый документ отслеживается отдельным таймером
- ✅ Все документы обрабатываются параллельно
- ✅ Статус каждого документа обновляется независимо
- ✅ Ошибки обрабатываются для каждого документа отдельно

## 🧪 Тестирование

Для проверки исправления:

1. Загрузите несколько файлов одновременно (3-5 файлов)
2. Проверьте, что все файлы появились в списке документов
3. Проверьте, что статус каждого документа обновляется независимо
4. Проверьте логи - должны быть сообщения о начале проверки статуса для каждого документа

## 📝 Измененные файлы

- `flutter_app/lib/services/chat_service.dart`
  - Добавлен `Map<String, Timer> _documentStatusTimers`
  - Обновлен метод `_checkDocumentStatus()`
  - Обновлен метод `uploadDocuments()`
  - Обновлены методы `removeDocument()`, `clearAllDocuments()`, `clearAllCache()`

